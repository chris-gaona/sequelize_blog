extends ../layout

block content
  h1= title
  p 
    | Are you sure you want to delete 
    em= comment.comment
    | ?
  form(action="/articles/" + article.id + "/comment/" + comment.id + "?_method=DELETE", method="post")
    a(href="/articles/" + article.id) No! Take me back!
    | 
    input(type="submit", value = "Yes, I am")
